[project]
name = "llm-gomoku"
version = "0.1.0"
description = "LLM-vs-LLM Gomoku (five-in-a-row) arena with OpenAI-compatible function calling"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
]
